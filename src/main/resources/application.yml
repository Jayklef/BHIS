server:
  port: 8007

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/books
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
